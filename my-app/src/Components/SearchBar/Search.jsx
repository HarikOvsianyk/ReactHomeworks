import React from 'react';
import { Input } from '../UI/Input/Input';

export const Search = () => {
    return (
        <Input
        id="searchInput"
        type="text"
        label="Enter participant name..."
      />
    )
}